/**
 * Type for describing widget change events. It's possible
 * to extend this by adding additional events to discriminated union type.
 */
export declare type WidgetChangeEvent = TimeContextEvent;
export interface TimeContextEvent {
    type: 'TIME_CONTEXT';
    data: DateTimeContext;
}
export declare type WidgetChangeEventType = WidgetChangeEvent['type'];
export declare type DateTimeContext = [Date, Date];
