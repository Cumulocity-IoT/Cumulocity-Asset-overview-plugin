import { OnInit } from '@angular/core';
import { ModalService, ApplicationPlugin } from '@c8y/ngx-components';
import { BehaviorSubject, Observable } from 'rxjs';
import { BsModalRef } from 'ngx-bootstrap/modal';
import * as i0 from "@angular/core";
export declare class InstallPluginComponent implements OnInit {
    private bsModalRef;
    private modal;
    plugins$: BehaviorSubject<ApplicationPlugin[]>;
    filterTerm$: BehaviorSubject<string>;
    filteredPlugins$: Observable<ApplicationPlugin[]>;
    selectedPlugins: ApplicationPlugin[];
    result: Promise<ApplicationPlugin[]>;
    onlyLatestPluginVersion: boolean;
    private _install;
    private _cancel;
    constructor(bsModalRef: BsModalRef, modal: ModalService);
    ngOnInit(): void;
    cancel(): void;
    install(): void;
    private filterContainString;
    static ɵfac: i0.ɵɵFactoryDeclaration<InstallPluginComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<InstallPluginComponent, "c8y-install-plugin", never, { "plugins$": "plugins$"; }, {}, never, never, false>;
}
